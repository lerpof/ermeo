import 'package:flutter/material.dart';

/// Default surface size for component golden tests.
const Size kBeGoldenSurfaceSize = Size(400, 200);

/// Larger surface for molecules that need more vertical space.
const Size kBeGoldenSurfaceSizeTall = Size(400, 320);

/// Surface size for bottom navigation bar goldens.
const Size kBeGoldenNavBarSize = Size(400, 120);
