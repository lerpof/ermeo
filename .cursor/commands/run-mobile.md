run-mobile
Run ermeo_mobile on a connected device or simulator via FVM.

```bash
cd apps/ermeo_mobile && fvm flutter run
```

To pick a device explicitly:

```bash
cd apps/ermeo_mobile && fvm flutter devices && fvm flutter run -d <device-id>
```
