run-mobile
Run beneesse_mobile on a connected device or simulator via FVM.

```bash
cd apps/beneesse_mobile && fvm flutter run
```

To pick a device explicitly:

```bash
cd apps/beneesse_mobile && fvm flutter devices && fvm flutter run -d <device-id>
```
